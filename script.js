const num = 266219;
const array = ("" + num).split("").reduce((acc, rec) => acc * rec); // разбиваем число на отдельные цифры и перемножаем их используя метод перебора
console.log((array ** 3 + "").slice(0, 2)); // с помощью оператора возводим в 3-ю степень и выводим в консоль первые две цифры полученного числа

